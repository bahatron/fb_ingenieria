// import "dotenv/config";
// import { expect } from "chai";
// import $clientManager, { Client } from "../src/domain/client/services/ClientManager";

// describe("Client Manager", () => {
//     it("can create clients", async () => {
//         const clientData: Client = {
//             name: "test_client",
//             visible: false,
//             description: "an optional description",
//         };

//         const saved = await $clientManager.persist(clientData);

//         return expect(clientData).to.deep.equal(clientData);
//     });
// });
