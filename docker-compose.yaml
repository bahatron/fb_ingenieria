version: '3.5'

services:
    firebase:
        container_name: fb_ingenieria_firebase
        build: ./firebase
        user: node
        command: sh -c '${DEPLOY_CMD}'
        # volumes:
        #     - ./firebase/app:/usr/node/app
        # env_file: .env
        ports:
            - '${PORT}:${PORT}'
